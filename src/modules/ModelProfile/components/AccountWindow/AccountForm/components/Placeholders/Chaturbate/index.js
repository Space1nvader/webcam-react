import React from 'react';
import AccountFrame from '../../AccountFrame';

const ChaturbateFrame = () => (
  <AccountFrame
    id={1}
    // key={accounts[0].id}
    // errors={findErrors(accounts[0].id)}
    // data={accounts[0]}
    // initialValues={initialValues}
  />
);

export default ChaturbateFrame;
