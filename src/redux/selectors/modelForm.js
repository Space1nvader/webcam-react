export const modelFormSelector = (state) => state.modelForm;

export const modelFormChangedSelector = (state) => state.modelForm.formChanged;

export const modelFormTabSelector = (state) => state.modelForm.currentTab;

export const modelFormModalSelector = (state) => state.modelForm.confirmModal;
